/** @type {import('tailwindcss').Config} */

export default {
  content: [
  "./src/**/*.{astro,html,js,jsx,ts,tsx,md,mdx}"
],


  /* Overwrites global.css
  theme: {
    extend: {

    fontFamily: {
      psx: ["Courier New", "monospace"],
      ui: ["Courier New", "monospace"],
      terminal: ["Courier New", "monospace"],
    },

      colors: {
        retro: {
          primary: "rgb(var(--color-retro-primary) / <alpha-value>)",
          secondary: "rgb(var(--color-retro-secondary) / <alpha-value>)",
          terminal: "rgb(var(--color-retro-terminal) / <alpha-value>)",
        }
      }
    }
  },

  */
  
  plugins: []
};


