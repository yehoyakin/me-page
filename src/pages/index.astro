---
import MyLayout from "../layouts/MyLayout.astro";
import SplitLanding from "../components/Hero/SplitLanding.astro";
import ContactPanel from "../components/Hero/ContactPanel.astro";
import HeaderInfo from "../components/Hero/InfoHeader.astro";
import { asset } from "../utils/asset";
---

<MyLayout title="Home">
  {/* 💡 BULLETPROOF BLUR SYSTEM */}
  <div class="fixed inset-0 w-full h-screen overflow-hidden bg-zinc-950">

    {/* 💡 FIXED BACKGROUND LAYER */}
    <div class="fixed inset-0 z-0 pointer-events-none style3d" style="filter: blur(4px); transform: scale(1.05);">
      <img
        src={asset("images/kbg.gif")}
        class="w-full h-full object-cover opacity-100"
        alt=""
      />
    </div>

    {/* 💡 Static dark overlay tint */}
    <div class="absolute inset-0 z-10 pointer-events-none" />

    {/* 💡 THE FIX: Grid layout that divides space by screen height fractions */}
    <div class="relative z-20 grid grid-rows-[auto_1fr] h-screen max-h-screen w-full overflow-hidden">

      <header class="absolute top-0 right-0 px-8 py-6 z-50">
        <ContactPanel />
      </header>

      {/* HEADER SECTION: Max height limits keep it from drowning out the splits */}
      <div class="w-full max-h-[75vh] min-h-[50vh] flex items-center justify-center overflow-hidden p-2">
        <HeaderInfo 
          name="Homepage"
          description="Joaquin Herrera / Game Developer"
          portraitSrc={asset("images/frontPage/me.webp")}
          theme="secondary"
        />
      </div>

      {/* SPLIT CONTENT: Takes up the entire remaining vertical viewport portion */}
      <div class="w-full max-h-[50vh] min-h-[25vh] flex items-center justify-center overflow-hidden p-2">
        <SplitLanding
          background=""
          left={{
            href: `${import.meta.env.BASE_URL}portfolio/`,
            tag: "My projects",
            title: "Portfolio",
            description: "Take a look at my Unity and Unreal Engine projects.",
            actionText: "View projects",
            imageSrc: asset("images/kbg2.gif"),
          }}
          right={{
            href: `${import.meta.env.BASE_URL}blog/`,
            tag: "My thoughts",
            title: "Blog",
            description: "Just my thoughts on videogames, game development and movies.",
            actionText: "Read articles",
            imageSrc: asset("images/kbg1.gif"),
          }}
          footerText="Pixel art by Kirokaze!."
        />
      </div>

    </div>
  </div>
</MyLayout>